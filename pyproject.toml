[project]
name = "michael"
version = "0.1.0"
description = "Grayscale-only theme for grayscale-forced displays and e-ink panels"
requires-python = ">=3.11"
dependencies = [
    "pillow>=11",
    "pygments>=2.19",
]

[dependency-groups]
dev = [
    "ruff>=0.8",
]

[tool.ruff]
line-length = 100
